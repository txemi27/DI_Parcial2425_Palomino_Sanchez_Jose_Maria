import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI_PlantillaExamenParcial2425',
  webDir: 'www'
};

export default config;
